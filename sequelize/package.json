{
  "name": "sequelize-server",
  "version": "1.0.0",
  "description": "node server with sequelize used as the orm",
  "main": "server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jesse",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.0",
    "express": "^4.15.4",
    "morgan": "^1.8.2",
    "pg": "^7.3.0",
    "sequelize": "^4.8.2"
  },
  "devDependencies": {
    "nodemon": "^1.12.0"
  }
}
